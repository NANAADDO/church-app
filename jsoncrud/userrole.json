{
    "fields": [
        {
            "name": "title",
            "type": "string"
        },
        {
            "name": "label",
            "type": "string"
        },
        {
            "name": "permissions",
            "type": "select",
            "options": {

            }
        }

    ],
    "foreign_keys": [

    ],
    "relationships": [

    ],
    "validations": [
        {
            "field": "name",
            "rules": "required|max:10"
        },
        {
            "field": "label",
            "rules": "required"
        }
    ,
        {
            "field": "permissions",
            "rules": "required"
        }
    ]
}
